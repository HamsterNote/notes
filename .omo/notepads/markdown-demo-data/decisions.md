# markdown-demo-data Decisions

## Approved decisions
- Metadata format: JSON (not YAML)
- Body format: Markdown
- Inline editing requires real-time reverse update to document string
- Add Vitest unit tests
- No persistence, no public library API changes
